---
deployment:
  tasks:
    - export DEPLOYPATH="${DEPLOYPATH:-$HOME/public_html/website_e1175695/}"
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/rsync -av --delete --exclude='.git' --exclude='.github' --exclude='node_modules' ./ "$DEPLOYPATH"
